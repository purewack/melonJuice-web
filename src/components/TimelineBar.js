import '../css/Time.css'

const TimelineBar = ({bar, number, debug}) => {
    return(<div 
        className='TimelineBar'
        style={{width:bar}}>
        {0 + number}
    </div>)
}

export default TimelineBar